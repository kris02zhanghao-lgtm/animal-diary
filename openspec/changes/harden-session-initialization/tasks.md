## 1. 会话服务可靠性

- [x] 1.1 新增可测试的 Promise 超时与有限重试工具
- [x] 1.2 为 `ensureSession` 增加超时、一次重试和并发请求合并
- [x] 1.3 保证失败及重试流程不清理或替换已有匿名身份

## 2. 首屏恢复交互

- [x] 2.1 App 捕获并展示超时或普通连接失败文案
- [x] 2.2 失败页增加“重新连接”按钮，点击后恢复加载态并重新初始化
- [x] 2.3 effect 增加卸载保护，避免过期请求更新界面

## 3. 验证

- [x] 3.1 为超时及重试工具补充单元测试
- [ ] 3.2 运行 `npm test`（已运行；被既有 `achievementRules.test.js` 失败阻塞，本次新增测试通过）
- [x] 3.3 运行 `npm run lint`
- [x] 3.4 运行 `npm run build`
- [x] 3.5 检查 diff，确认未引入 session 清理逻辑
